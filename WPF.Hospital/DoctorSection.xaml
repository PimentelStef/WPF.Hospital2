<Window x:Class="WPF.Hospital.DoctorSection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="DoctorSection" Height="450" Width="800">
    <Grid>
        <GroupBox Header="Doctor"
          FontWeight="Bold"
          Padding="10"
          Margin="10">

            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Doctor Name"/>
                    <TextBox 
                        Width="750"
                        Height="30"
                        Margin="0,5"
                        Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <Button Grid.Row="2"
                Grid.ColumnSpan="2"
                Content="Add Doctor"
                Height="35"
                Margin="5"
                Background="#9B59B6"
                Foreground="White"
                FontWeight="Bold"
                Click="btnAddDoctor_Click"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
