<Window x:Class="WPF.Hospital.PrescriptionSection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="PrescriptionSection" Height="450" Width="800">
    <Grid>
        <GroupBox Header="Prescription"
          FontWeight="Bold"
          Padding="10"
          Margin="10">

            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Medicine:"
                   VerticalAlignment="Center"/>

                <ComboBox
                    Width="600"
                    Height="30"
                    Margin="0,5"
                    Grid.Column="1"
                    ItemsSource="{Binding Medicines}"
                    SelectedValue="{Binding SelectedMedicineId}"
                    SelectedValuePath="Id"
                    DisplayMemberPath="DisplayName"/>

                <TextBlock Grid.Row="1"
                   Text="Quantity:"
                   VerticalAlignment="Center"/>

                <TextBox Grid.Row="1"
                 Grid.Column="1"
                 x:Name="txtQuantity"
                 Height="30"
                 Margin="5"
                 Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2"
                   Text="Frequency:"
                   VerticalAlignment="Center"/>

                <TextBox Grid.Row="2"
                 Grid.Column="1"
                 x:Name="txtFrequency"
                 Height="30"
                 Margin="5"
                 Text="{Binding Frequency, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Row="3"
                Grid.ColumnSpan="2"
                Content="Add Prescription"
                Height="35"
                Margin="5"
                Background="#27AE60"
                Foreground="White"
                FontWeight="Bold"
                Click="btnAddPrescription_Click"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
